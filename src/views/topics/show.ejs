<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <% include ../static/partials/head.ejs %>
  </head>
  <body>
    <% include ../static/partials/navbar.ejs %>

    <main class="container">
      <h1><%= topic.title %></h1>
        <!-- maybe fix the list flairs below-->
        <ul class="list-flairs">
          <% topic.flairs.forEach((flair) => { %>
            <li class="list-flair-item">
              <a class="flair-item"style="background-color:<%= flair.color %>;"href="/topics/<%= topic.id %>/flairs/<%= flair.id %>"> <%= flair.name %> </a>
            </li>
        <% }) %>
        </ul>
        <a href="/topics/<%= topic.id %>/edit" class="btn btn-warning">Edit Topic</a>
        <form style="display:initial" action="/topics/<%= topic.id %>/destroy" method="post">
          <button type="submit" class="btn btn-danger">Delete Topic</button>
        </form>
        <a href="/topics/<%= topic.id %>/posts/new" class="btn btn-success">New Post</a>
        <a href="/topics/<%= topic.id %>/flairs/new" class="btn btn-primary">New Flair</a>
      <p><%= topic.description %></p>
      <hr>
      <h1>Posts</h1>

      <ul class="list-group">
        <% topic.posts.forEach((post) => { %>
          <li class="list-group-item">
            <a href="/topics/<%= topic.id %>/posts/<%= post.id %>"> <%= post.title %> </a>
          </li>
        <% }) %>
      </ul>
    </main>

    <% include ../static/partials/baseScripts.ejs %>

  </body>
</html>
